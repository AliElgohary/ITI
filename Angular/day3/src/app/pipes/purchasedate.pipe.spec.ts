import { PurchasedatePipe } from './purchasedate.pipe';

describe('PurchasedatePipe', () => {
  it('create an instance', () => {
    const pipe = new PurchasedatePipe();
    expect(pipe).toBeTruthy();
  });
});
